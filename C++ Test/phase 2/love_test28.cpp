#include <bits/stdc++.h>
using namespace std;

int main() {
    int arr[] = {11, 21, 31, 41};
    // int *ptr = arr++;   // here we are trying to update arr on symbol table. THat is not possible. So, error occured
    // cout << *ptr << endl;
    return 0;
}